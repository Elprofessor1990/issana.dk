# ISSANA

Website for ISSANA — Rådgivende ingeniører.

The site is a lightweight static build published with GitHub Pages at
[issana.dk](https://issana.dk).

## Local preview

Run a static web server from this directory, for example:

```powershell
python -m http.server 4173
```

Then open `http://localhost:4173`.
